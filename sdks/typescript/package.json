{
  "name": "quiver-client",
  "version": "0.1.0",
  "description": "TypeScript client for the Quiver HTTP server",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "files": [
    "dist"
  ],
  "keywords": ["vector-database", "embeddings", "nearest-neighbor", "ai", "quiver"],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.4.0",
    "msw": "^2.2.0",
    "@vitest/coverage-v8": "^1.4.0"
  }
}
